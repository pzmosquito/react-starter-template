import * as React from "react";
import { observer } from "mobx-react-lite";


const Header = () => (
    <div className="title">
        Header
        <hr />
    </div>
);

export default (observer(Header));
